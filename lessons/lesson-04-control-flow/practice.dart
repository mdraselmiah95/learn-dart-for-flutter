// ============================================================
//  Lesson 04 — Practice Tasks
//  Try each task yourself before checking solution.dart
// ============================================================

void main() {

  // ── TASK 1 ────────────────────────────────────────────────
  // Create int number = 7.
  // Print 'Even' if it is even, 'Odd' if it is odd.
  // Hint: use % 2


  // ── TASK 2 ────────────────────────────────────────────────
  // Create int marks = 82.
  // Print the grade using if/else if/else:
  // 90+ → A+, 80+ → A, 70+ → B, 60+ → C, below 60 → F


  // ── TASK 3 ────────────────────────────────────────────────
  // Create String month = 'July'.
  // Use switch to print the season:
  // December/January/February → Winter
  // March/April/May → Spring
  // June/July/August → Summer
  // September/October/November → Autumn


  // ── TASK 4 ────────────────────────────────────────────────
  // Create int balance = 500 and int price = 750.
  // Use a ternary to print 'Purchase successful'
  // or 'Insufficient balance' in ONE line.


  // ── TASK 5 (Challenge) ────────────────────────────────────
  // A shop gives discounts:
  // - member AND total >= 100  → 20% discount
  // - member only              → 10% discount
  // - total >= 100 only        → 5% discount
  // - neither                  → no discount
  // With bool isMember = true and double total = 150,
  // calculate and print the final price.
  // Expected output: Final price: 120.0

}
