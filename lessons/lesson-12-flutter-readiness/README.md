# Lesson 12 — Flutter Readiness

## Topics Covered
- Dart packages: `pubspec.yaml`, `dart pub get`, using pub.dev
- Building a data model class with `fromJson` / `toJson`
- Writing a service class for data fetching
- Using `final`, `const`, and immutable models
- Understanding the separation: models / services / logic
- Building a small Dart app that mirrors Flutter app architecture
- Dart patterns you will see on day 1 of Flutter

## What You Will Build
A small Dart console app that:
- Defines a `User` model
- Has a `UserService` that fetches and stores users
- Parses JSON-like Map data into model objects
- Filters and displays user data

This is exactly the pattern used in Flutter apps.

## Flutter Connection
This lesson bridges Dart and Flutter directly. After this lesson, Flutter code will look familiar — you already know the Dart patterns it is built on.

## How to Run
```bash
dart run lessons/lesson-12-flutter-readiness/main.dart
```

> Files coming soon. See [roadmap](../../roadmap.md) for lesson schedule.
