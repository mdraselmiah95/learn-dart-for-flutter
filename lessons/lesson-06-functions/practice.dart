// ============================================================
//  Lesson 06 — Practice Tasks
//  Try each task yourself before checking solution.dart
// ============================================================

void main() {

  // ── TASK 1 ────────────────────────────────────────────────
  // Write a function isEven(int n) that returns true/false.
  // Test it with 4 and 7.


  // ── TASK 2 ────────────────────────────────────────────────
  // Write a function bmi({required double weightKg, required double heightM})
  // that RETURNS the BMI (weight / (height * height)).
  // Call it with named arguments and print the result.


  // ── TASK 3 ────────────────────────────────────────────────
  // Write an arrow function celsiusToFahrenheit(double c)
  // Formula: c * 9 / 5 + 32
  // Print the result for 0, 25, and 100 degrees.


  // ── TASK 4 ────────────────────────────────────────────────
  // Given: List<int> prices = [120, 45, 89, 200, 15];
  // Use .where() to keep prices above 50,
  // then .map() to add 10% tax to each,
  // then print the result.


  // ── TASK 5 (Challenge) ────────────────────────────────────
  // Write a function applyToAll(List<int> items, int Function(int) fn)
  // that returns a NEW list with fn applied to every item.
  // (Build it with a loop — don't use .map())
  // Test it: applyToAll([1, 2, 3], (n) => n * 10) → [10, 20, 30]

}
