// ============================================================
//  Lesson 09 — Practice Tasks
//  Try each task yourself before checking solution.dart
// ============================================================

void main() {

  // ── TASK 1 ────────────────────────────────────────────────
  // Create a class Book with: String title, String author, double price.
  // Add a constructor using this. shorthand.
  // Create 2 books in main() and print their titles.


  // ── TASK 2 ────────────────────────────────────────────────
  // Add a method summary() to Book that prints:
  // '[title] by [author] — $[price]'
  // Call it on both books.


  // ── TASK 3 ────────────────────────────────────────────────
  // Add a named constructor Book.free(String title, String author)
  // that sets price to 0.
  // Create a free book and print its summary.


  // ── TASK 4 ────────────────────────────────────────────────
  // Create a class Counter with:
  // - a PRIVATE int _count starting at 0
  // - a getter 'count'
  // - methods increment() and reset()
  // In main: increment 3 times, print count (→ 3), reset, print (→ 0).


  // ── TASK 5 (Challenge) ────────────────────────────────────
  // Create a class Rectangle with final double width and height.
  // Add: a const constructor,
  //      a getter 'area' (width * height),
  //      a getter 'isSquare' (width == height),
  //      a toString override.
  // Test with Rectangle(4, 4) and Rectangle(3, 5).

}
