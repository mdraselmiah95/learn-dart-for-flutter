// Exercise 03 — Control Flow
// Difficulty: Beginner · Related lesson: lesson-04
//
// Run: dart run exercises/exercise-03-control-flow.dart

void main() {
  // TASK A: Traffic light
  // String light = 'yellow';
  // switch: red → 'Stop', yellow → 'Slow down', green → 'Go',
  // default → 'Broken light?'

  // TASK B: Number describer
  // int n = -7;
  // Print whether it is: positive/negative/zero AND even/odd.
  // → -7 is negative and odd

  // TASK C: Shipping cost
  // double orderTotal = 850;
  // >= 1000 → free shipping
  // >= 500  → 50 taka
  // below   → 120 taka
  // Print with a ternary OR if/else chain.

  // TASK D: Login simulator
  // String? username = 'rasel'; String? password = null;
  // If both non-null → 'Welcome', if username only → 'Enter password',
  // else → 'Enter credentials'
}
